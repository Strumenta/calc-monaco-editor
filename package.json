{
  "name": "calc-monaco-editor",
  "version": "0.0.1",
  "author": "Strumenta",
  "license": "Apache-2.0",
  "repository": "https://github.com/Strumenta/calc-monaco-editor",
  "dependencies": {
    "antlr4": "^4.7.2",
    "monaco-editor": "^0.17.1"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "mocha": "^6.2.0",
    "webpack-cli": "^3.3.7"
  },
  "scripts": {
    "all": "npm install && npm run generateParser && npm run compile && npm run test && npm run bundle && npm run serve",
    "generateParser": "./gradlew generateParser",
    "compile": "tsc",
    "test": "mocha",
    "bundle": "webpack",
    "serve": "cd server && ../gradlew runServer"
  }
}
